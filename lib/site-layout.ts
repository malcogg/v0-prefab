/**
 * Top padding for page content below the fixed site header
 * (accent bar + announcement + nav + context strip).
 */
export const SITE_HEADER_CLEARANCE = "pt-44 sm:pt-44"

/** Extra space above breadcrumb trails so they clear the context strip comfortably. */
export const SITE_BREADCRUMB_TOP = "pt-3 sm:pt-4"
